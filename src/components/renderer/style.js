const styles = theme => ({
  renderer: {},
  flex: {
    flex: 1
  },
  domparent: {
    minHeight: '300px',
    color: '#FFF',
    fontFamily: theme.typography.fontFamily,
    fontSize: theme.typography.fontSize
  }
})

export default styles
